# AGENTS.md

给在本仓库工作的 agent 的操作说明。

## 这个仓库在做什么

分析 PDF 的体积构成，定位**冗余**，在不损失信息的前提下去掉它。**逐个案例**推进，
每个案例留下可复现的完整记录。

核心前提：**文件不会凭空获得信息。** 一份 PDF 经某道处理后变大，增加的字节要么是
真的新信息（罕见），要么是冗余（常见）。工作就是把每个字节归位到具体对象，然后
判定它属于哪一类。

## 环境

本仓库在 Windows + Anaconda 下开发，但工具本身只依赖 Python 库，跨平台可用。

| 用途 | 说明 |
|---|---|
| Python | 用 conda 环境里的解释器直接调用；**不要 `conda activate`**，在非交互 shell 里会失败 |
| 中文输出 | 设 `PYTHONIOENCODING=utf-8`，否则 Windows 下按 cp1252 编码会报错 |
| JBIG2 编码器 | `pdf_optimize.py` 依次从 `$JBIG2_BIN`、`PATH`、名为 `pdfopt` 的 conda 环境查找；也可用 `--jbig2-bin` 指定 |
| poppler | `<conda-prefix>/Library/bin/`（`pdfinfo` `pdfimages` `pdffonts` `pdftoppm`） |
| Python 库 | `pikepdf` `PyMuPDF(fitz)` `pypdf` `Pillow` `numpy` `scipy` |

安装 jbig2enc：

```bash
conda create -n pdfopt -c conda-forge jbig2enc -y
```

开发机上**没有** `qpdf` / `ghostscript` / `ImageMagick` CLI，所以所有 PDF 改写都走
pikepdf。如果你的环境里有这些工具，不必刻意回避，但别把它们变成必需依赖。

### 云同步目录要当心

待处理的 PDF 常常放在云同步目录里（Dropbox / OneDrive / iCloud 之类）。这类目录
下的文件可能是「仅在线」占位符，**不要**做递归列举或属性扫描（`ls -la`、`ls -R`、
`Get-ChildItem -Recurse`）——那会触发系统把大量无关文件从云端拉下来，占用磁盘和
带宽。用 `dir /b <path>` 这类窄命令，只读取任务真正需要的那几个文件。

往同步目录写成品前先问用户，那会触发上传。

## 方法：四步，不许跳步

### 1. 字节归属核算

```bash
python tools/pdf_anatomy2.py file.pdf
```

**先回答字节在哪儿，再决定动什么。** 直接上「压缩工具」是赌博。

**核算必须闭合**：各项增量之和 == 文件大小差值。对不上说明还有没找到的东西，
继续找，不要糊弄过去。上一个案例算出
`54,170,873 + 36,968,402 = 91,139,275`，与实际差值分毫不差——这才叫定位清楚了。

注意 `pdf_anatomy.py`（v1）**无法给非流对象计价**，字典会显示 0 字节。标签树、
注释这类东西压缩在对象流里，必须用 v2。对象数上十万时 pikepdf 逐个解引用很慢，
用抽样（每 N 个取 1）先拿结论。

### 2. 逐页编码对比

```bash
python tools/pdf_page_diff.py before.pdf after.pdf
```

**必须递归进 Form XObject。** 很多 OCR 引擎（ABBYY 就是）把页面图像包在 Form
XObject 里；不递归的话页面 `/Resources/XObject` 里一张图都找不到，会得出「图像
消失了」的错误结论。

### 3. 判定新增字节是否携带新信息

这是全流程的判据所在，**不能靠推测，要做像素分析**：

- 8-bit 灰度页里 95% 像素是纯黑或纯白、中间灰阶集中在笔画边缘
  → JPEG 振铃伪影，源头是 1-bit，信息增量为零 → **冗余，可去**
- 真彩色照片、插图、有真实灰度层次的页面 → **真信息，不许动**

### 4. 最小改动 + 强制验证

```bash
python tools/pdf_optimize.py in.pdf out.pdf --source original.pdf --strip-tags
python tools/pdf_verify.py in.pdf out.pdf
```

改动越小越好。上个案例只替换了图像 XObject 的像素载荷，不动几何、不动对象引用，
所以不可见文字层的对齐关系天然保持。

## 硬性规则

1. **没有证据不许说「无损」。** 要么给出流的 SHA256 与原始相同，要么给出渲染像素
   比对。上个案例是 136/137 页 SHA256 字节相同——这才是证明。

2. **验证必须包含渲染像素。** 只查结构会漏掉两类致命错误：
   - CCITT 的 `BlackIs1` 弄反 → 整页反色，而页数、几何、文字层检查**全部通过**
   - 图像错位 → 文字层与图像对不上
   `tools/pdf_verify.py` 四项都查（页数/几何、文字层逐页、渲染 MAE、反色检测）。

3. **在原生分辨率比对像素。** 300 dpi 的位图降到 150 dpi 比较会产生 8–10% 的假
   差异（降采样改变抗锯齿判定），看着像损坏其实没有。比对墨量（暗像素计数）时
   尤其要用原生 dpi。

4. **区分「标签树」和「可搜索文字」。** OCR 文字在页面内容流里（不可见渲染模式
   `3 Tr`），不在 `/StructElem` 里。删标签树不影响搜索/复制/导出，只影响屏幕
   阅读器和重排。**向用户解释清楚再让他选**，不要替他决定。

5. **有原始文件就移植原始流**，不要重新二值化。移植是逐字节无损；重新二值化要过
   阈值判断，必然引入误差。

6. **注意参照物是谁。** 拿「清晰的 1-bit 结果」去比「模糊的灰度 JPEG 参照」，边缘
   必然有差异，而且**我们的更忠实**。这种页要改用真正的源文件做逐像素比对。

7. **OCR 常常丢书签。** 处理完检查 `get_toc()`，有丢就从原始文件移植。移植时
   **必须重建目的地**：命名目的地跨文档无法解析，`set_toc` 会静默退化成 page −1
   ——书签看着在，点了不跳。验证要核对**页码**，不能只数条数。

## 位图编码选择

对 1-bit 扫描文本页：**JBIG2 < CCITT G4 ≪ 灰度 JPEG**。

**JBIG2 只用通用模式（`-p`，不加 `-s`）。** 符号模式再小约 11%，但它把视觉相似
的字形聚类共用一个符号，可能把一个字替换成另一个字——施乐扫描仪数字调包事故
就是这么来的。其无损变体 `-r` 在 jbig2enc 上游已损坏，运行会直接拒绝。通用模式
是对位图做上下文算术编码，构造上无损，实测每页比 CCITT G4 小 26%。

图文混排页考虑 MRC（文字层二值化 + 图片层 JPEG 分层），这是商业 OCR「高压缩」
模式的原理。

## 案例目录约定

```
cases/<短横线-slug>/README.md
```

每个案例记录必须包含：文件大小对照表、**闭合的字节归属核算**、冗余判定的像素
证据、修复策略与变体对照、**验证输出原文**、结论与可迁移经验。

数据一律贴实测输出，不要复述或概括数字。

**但不要写进去**：源文件的书名/标题、文件名里的馆藏号或书号、正文摘录、目录与
书签的条目文字、以及任何本地绝对路径。案例记录的价值在于测量数据和方法，不在于
是哪一本书。用 `original.pdf` / `ocr.pdf` / `optimized.pdf` 指代文件，标题类内容
一律写成 `<已隐去>`。

## 工具

| 脚本 | 用途 |
|---|---|
| `tools/pdf_anatomy.py` | 快速分类统计（仅流对象，早期版本） |
| `tools/pdf_anatomy2.py` | 完整字节归属，含对象流内字典计数 |
| `tools/pdf_page_diff.py` | 逐页编码转换对比（递归 Form XObject） |
| `tools/pdf_optimize.py` | 移植原始位图 / 重新二值化 / JBIG2 / 剥离标签树 / 重压缩 |
| `tools/pdf_verify.py` | 页数、几何、文字层、渲染像素四重验证 |
| `tools/pdf_bookmarks.py` | 查看 / 跨文件移植书签（重建目的地并校验页码） |

## 每个案例都要做联网检索

用户明确要求：**每个案例分析时都到互联网上系统性检索该类冗余的处理方法**，
不要只依赖已有知识。检索到的结论写进案例记录并附来源链接。见
`.claude/skills/pdf-case/SKILL.md`。
